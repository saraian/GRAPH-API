# Pack A baseline tours

Five short single-floor HM3D runs with native semantic GT. Each run has two complete laps. Every object action occurs on lap 1; lap 2 repeats the complete route with no actions and is the confirmation pass. Run the external acquisition adapter with `--laps 2`. All action triggers are within 4 m. PNG changes are expected inputs, not recorded baseline outcomes.
