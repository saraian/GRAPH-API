# Pack B baseline tours

Five different single-floor HM3D runs with native semantic GT. Each run has three complete laps. Independent object lifecycles are distributed across laps 1 and 2; lap 3 repeats the complete route with no actions and is the confirmation pass. Run the external acquisition adapter with `--laps 3`. All action triggers are within 4 m. PNG changes are expected inputs, not recorded baseline outcomes.
