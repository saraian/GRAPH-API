# Merge algorithm investigation — 2026-09-14

Lane: merge-algorithm (session `01H8nfi5VvLzhqAzx3axvsDG`, Claude Code). Handed over from the
perception lane with the instruction to investigate and debug the merge algorithm only.
Owner statement mid-session: "We understood that the major of performance is dropped there."

Subject: the code that runs in the shared tree on 2026-09-14, not the committed code.
`object_services.py` SHA-256 `e096a9a9…f95d41` was copied byte-for-byte into the worktree
`/DATA/GRAPH-API/.claude/worktrees/found-merge-algorithm` (branch
`worktree-found-merge-algorithm`, baseline commit `16880b0`) before any edit.

## Result

**The merge stage cannot repair the duplicates that the association stage creates. This is by
construction, not by tuning.** Both stages use the same attribute-similarity function
(`lost_similarity_detailed`, `/DATA/GRAPH-API/lost3dsg/src/perception_module/nlp_utils.py:340`).
The description term carries 50 % of the weight. Two VLM descriptions of one object seen from two
views have a sentence-embedding cosine of 0.36–0.83 (exact, n = 21, see below). The merge gate
(0.925) needs that cosine to be at least 0.850 even when label, colour and material all agree.
Zero of the 21 measured pairs reach it. The gate has no geometric term at all: a pair with
identical fused boxes (IoU 1.000, 0.00 m apart, same label, same colour word, same material word)
was refused on similarity 0.889.

The association gate (0.85) needs the same cosine to be at least 0.700. The median measured value
is 0.685. So a re-observation of a known object is refused by association (new object created),
and the merge, which is required to be strictly stricter (GA-341), can never take it back.
This is why the GA-493 run applied 119 additions and 20 updates for 139 observations, and why 84
of 104 final objects have exactly one view.

## Evidence

All numbers are from ONE bundle: the GA-493 debug run, scene 00824, at
`/DATA/GRAPH-API/.handoff/lanes/perception/ga493_replay_20260914/bundle/`. It is a capped
partial tour. Every count below states its denominator.

### Decision counts (`hook_decisions.jsonl`)

| Kind | Count |
|---|---:|
| merge sweeps (`not_offered_summary` rows) | 13 |
| pairs never offered by the AABB broad phase | 16 789 |
| `merge` applied | 15 |
| `merge_refused`, reason `distance` | 1 105 |
| `merge_refused`, reason `similarity` | 589 |
| `merge_refused`, reason `room` | 165 |
| `merge_refused`, reason `already_condemned` | 54 |
| distinct refused pairs | 559 |

The same pair is re-refused with the identical score on every sweep: median 2 repeats per
(pair, reason), maximum 13; 158 keys were refused 5 or more times. Nothing about such a pair
changes between sweeps, so the repetition measures nothing and merges nothing.

### Similarity refusals versus the pair's own geometry (GT-free)

For every refused pair whose two sides both survive in `persistent_perception.json`, I computed the
3D IoU of their final `fused_bbox`. This does not use ground truth.

| Reason | distinct pairs with both boxes | same base label | same label AND boxes overlap |
|---|---:|---:|---:|
| similarity | 164 | 35 | **31** |
| room | 51 | 26 | 14 |
| distance | 259 | 54 | 13 |

Top overlapping same-label pairs refused on **similarity** (fused IoU, recorded score):
`cabinet#2`/`cabinet#2` 1.000 @ 0.889; `chair#1`/`chair#1` 0.908 @ 0.763; `chair#1`/`chair#1`
0.852 @ 0.843; `chair#2`/`chair#2` 0.808 @ 0.847; `chair#2`/`chair#2` 0.786 @ 0.763;
`chair#1`/`chair#1` 0.778 @ 0.833; `chair#2`/`chair#2` 0.720 @ 0.842; `table#1`/`table` 0.654 @
0.864; `plant`/`plant` 0.601 @ 0.698; `mirror`/`mirror` 0.572 @ 0.773; `bench`/`bench` 0.550 @ 0.739.

Raw record check (rule 1): the two `chair#1` objects `obj_00ec167c…` and `obj_3dde2c36…` have
measured boxes overlapping at IoU 0.457, label `chair`, material `fabric` on both sides, colour
`grey` versus `brown`, descriptions "dark grey tufted armchair with nailhead trim" versus "dark
brown armchair". Score 0.763, refused on all 11 sweeps that offered the pair.

### The description term, solved exactly

Weights (`config.yaml`, `similarity:`): label 0.05, colour 0.30, material 0.15, description 0.50.
For a pair whose label, colour word and material word are identical strings, the code returns 1.0
for those three terms without consulting any model (string short-circuit in `semantic_similarity`
and `color_similarity_rgb`). The recorded score then gives the description cosine D exactly:
`D = (score − 0.50) / 0.50`. For pairs with material `unknown` on both sides the term is dropped
and `D = (0.85·score − 0.35) / 0.50`.

Over the 21 same-label overlapping pairs refused on similarity that satisfy this condition:
**D min 0.364, median 0.685, max 0.827. The merge gate needs D ≥ 0.850. Zero reach it.**
The identical-box `cabinet#2` pair had D = 0.778.

Ceiling: with everything else perfect, `score = 0.50 + 0.50·D`. At the best measured D (0.827) the
score is 0.914, below the merge gate. The 15 merges that did apply are pairs whose description
strings were identical or nearly so (`rug`/`rug` scored 1.000 three times).

I could not recompute the scores with the real model on this host (`webcolors` and
`sentence_transformers` are absent; the test stub replaces both and returns no embedding). The
derivation above uses only the recorded scores and string equality, so it does not depend on
the model. Rule 2: the stub's "repro" column in my first script was discarded for that reason.

### Room gate

57 distinct pairs refused on room. 56 are `room_4` versus `room_5`, one boundary that runs through
the kitchen cabinet run. 7 of the 57 scored at or above 0.925 and would otherwise have merged; 4 of
those are same-label cabinets whose measured boxes overlap (IoU 0.03–0.14, fused 0.14–0.36). The
gate reads `room_at_bbox(obj.bbox)`, centre first then four corners, so two overlapping boxes can
be filed in different rooms.

### Distance gate

Fixed 0.8 m centre-to-centre distance, on `obj.bbox` (the single measured view). 46 same-label
pairs refused at a median 1.27 m. A large object seen in two halves has centres further apart than
0.8 m by construction (`rug`/`rug`: fused IoU 0.30, centre distance 1.57 m). The merge sweep never
reads `fused_bbox` (`grep` over `_cb_merge_objects`: no reference). For the chair pairs above,
measured-box IoU is 0.44–0.61 while fused-box IoU is 0.78–0.91.

### The applied merges

6 of the 15 applied merges are `cabinet`↔`cabinet` at similarity 0.928–0.975 whose keeper sits
3.4–4.4 m from the nearest GT `cabinet` with IoU 0.000. The GT names those objects `kitchen
cabinet` (8), `kitchen lower cabinet` (4), `cabinet` (2). Whether those six merges were correct
cannot be decided from this bundle; label-vocabulary mismatch is a separate matter.

## Defects fixed (worktree commit `f02de48`)

1. **`merged_count` reported decided pairs, not applied merges.**
   `/DATA/GRAPH-API/lost3dsg/src/perception_module/object_services.py`, `_cb_merge_objects`,
   apply block under `with wm.lock:`. A pair whose keeper left the map between the unlocked sweep
   and the locked write was skipped and still counted. A pair whose **discard** left the map was
   not even counted as stale: it fell through to the room-graph update and was reported as a
   merge. The caller (`object_manager_6.py`, `_scan_merge`) logs this number as "merge(s) applied"
   and the GA-493 replay capture records it. Now both cases count as stale and `merged_count` is
   the applied count. A dry run keeps the decided count, because it applies nothing and its
   decisions are its output.
   Check: the GA-393 smoke check now asserts `merged_count == 1` on a clean merge and `== 0` when
   the discard is removed during the sweep. Negative control: on the pre-fix bytes the new
   assertion fails with `a stale pair was counted as a merge: 1`.
2. **`[MERGE AABB] examined=` was multiplied by the hit count.** `_merge_candidates`, legacy
   branch, added `DetectionIndex.last_examined` (a per-query value) once per hit. Diagnostic
   only; no decision reads it.
3. **Two stale tests.** `/DATA/GRAPH-API/lost3dsg/test/test_free_om6.py:137,144` and
   `/DATA/GRAPH-API/lost3dsg/test/test_ga11_merge_survivor.py:33` asserted
   `merge_duplicate_objects(...) is True`. The method returns the applied count (an `int`); its one
   production caller consumes it as a count. `1 is True` is false, so both tests failed on every
   run. They now assert `== 1`.

Verification: `ruff check` clean on every file I edited (the smoke test carries 2 pre-existing
fixable findings that are byte-identical on the baseline). `test_free_om6.py` PASS,
`test_ga11_merge_survivor.py` PASS, `test_scan_merge.py` PASS, `test_bbox_fusion.py` PASS,
`test_bbox_fusion_parallel.py` PASS. `test_perception_smoke.py` 35/36, identical to the baseline;
the one failure is the pre-existing GA-128 install-list item, outside the merge.
Not pushed. No simulator run, model call, or shared-tree edit.

## Not changed — the owner's decisions

None of the following is a bug in the code as written. Each is a design choice that the
measurements above show is where the map quality is lost. Changing any of them changes every
decision the system makes, so they need the owner's word.

| Option | What it does | Cost |
|---|---|---|
| A. Switch `association.merge_engine` to `evidence` | Uses the already-written engine in `association.py`: overlap, separation, co-visibility, ontology, room channels fused as log-odds. It has **no description or colour term**, so the ceiling above does not apply. The `channel_overlap` docstring cites a corpus replay over 17 evidence-engine bundles. | Needs `merge_min_consecutive` = 2 sweeps while the pair stays in view (the perception report measured that pairs leave view first). Changes every merge decision. No run since GA-296 gave objects a centroid. |
| B. Add a geometric acceptance to the legacy gate | Accept a same-base-label pair whose measured (or fused) IoU exceeds a threshold, regardless of the description score. Smallest code change. | Introduces a new constant that must be measured, not chosen. Does not touch the association gate, so duplicates are still born and cleaned later. |
| C. Lower the description weight, or cap D | Removes the ceiling in both stages. | Also loosens association; a red book and a blue book at one spot would merge on label alone (the GA-21 case) unless colour is weighted up. |
| D. Room gate reads box overlap | Refuse on room only when the two boxes do not overlap. | Small. Affects 7 pairs in this bundle. |
| E. Distance gate scales with extent, or uses box gap | Lets two halves of a rug or a sofa meet. | Affects 46 same-label pairs here; needs a measured constant. |

My recommendation is A, because it is the only option that already exists in the tree, already
excludes the term that the evidence shows is the ceiling, and already has a corpus replay behind
it. B is the cheapest experiment if A is too broad for the next run.

## Latent finding, not fired in this run (rule 14)

`SemanticEmbedder.__init__` and `.encode` in `nlp_utils.py` swallow every exception. If the
sentence-transformer fails to load in the container, every non-identical string scores 0.0 and
every description embedding is `None`; the description term is then dropped and a same-label,
same-colour pair scores 1.0 (the GA-101 shape). The recorded scores in this bundle are
non-trivial, so the real model answered here. The handler remains.

## Association stage — same method, same bundle (added on the owner's request)

Method: rebuild the world model in time order from the mutation ledger (119 adds, 19 updates, 1
direct update), the 15 merges and 24 disappearance removals, and the captured consumer inputs
(226 detections with 3D box, label, colour, material, description). The replay reproduces the
final object count (104). At each ADD, the existing objects are tested against the new box with
the gate the legacy loop applied at that moment: `locality_ok` at IoU 0.10 in exploration and
0.30 in tracking (`/DATA/GRAPH-API/lost3dsg/src/perception_module/object_manager_6.py`,
`locality_ok`, then `lost_similarity > 0.85`). `obj.bbox` in that loop is the last accepted
view's AABB, and that is what the replay keeps. Tool: `tools/assoc_replay.py`. GT-free.

| At the moment of the ADD, the nearest same-label object … | adds | expl | track |
|---|---:|---:|---:|
| A. overlapped **at or above** the locality gate → only the attribute score could refuse | 9 | 1 | 8 |
| B. overlapped **below** the locality gate (0 < IoU < 0.30 in tracking) | **25** | 0 | 25 |
| C. was within 1 m with no overlap | 16 | 3 | 13 |
| D. was not within 1 m (first sighting of that class here) | 69 | 12 | 57 |

So 50 of 119 new objects were created while a same-label object stood within 1 m.

**B is the largest association leak, and it is geometry, not attributes.** 16 of the 25 have IoU
in [0.10, 0.30): they would have passed the exploration gate and were refused only because the
run was in tracking mode. The partner's colour word and material word are identical in almost
every row (cabinet white/wood → white/wood, rug grey/fabric, sofa blue/fabric, mirror
brown/glass, pillow blue/fabric), centre distances 0.07–0.50 m. Two measured single-view boxes of
one object overlap little because each view sees a different surface; the merge analysis above
measured 0.44–0.61 for the chair pairs on measured boxes against 0.78–0.91 on fused boxes. The
locality gate reads the measured box.

**A is the same attribute ceiling as the merge.** Mirror at IoU 0.90 ("wooden framed mirror
reflecting a room" versus "rectangular mirror with brown frame"), `chair#1` at 0.86 and
`chair#2` at 0.79 (grey/fabric versus brown/fabric, the same two armchairs the merge could not
fuse), plant at 0.52, bench at 0.40. Colour word identical in 5 of 9, material in 8 of 9.

**C** is the adjacent-fragment case (table at 0.18 m, `cabinet#3` at 0.21 m, rug at 0.45 m):
no overlap, so no gate after the broad phase ever compared them.

`check_tracking_transition` (the "a stable object moved" path) uses the same attribute score at
0.85 and fires only when distance > 0.35 m and IoU < 0.10. It fired once in this run (sofa,
0.933 m, `update` record `change: moved`).

The manager also discarded 85 of 226 detections before association (`observation_discard`,
`manager_motion_gate`). That is upstream of both stages and is the perception lane's finding #2.

## Owner rulings (2026-09-14) and what the data said about them

Rulings, in the owner's words: "Room gate > B geometry (geometry filters boxes out) + A but without
Ontology (will be used in FOUND, not in current paper) and still use the LSF metric (so + C is
needed)"; for association, "same geometry filter + same LSF as merge"; for C, "you choose from the
data"; push the branch.

### C, chosen from the data (`tools/sweep_c.py`, real MiniLM, offline cache, GA-493 bundle)

Positives (same object, GT-free): the 15 applied merges, the 31 same-label overlapping pairs
refused on similarity, and 55 association cases where a same-label object overlapped the new
detection at admission (101 pairs). Negatives: 306 same-label final pairs more than 1.5 m apart
with no overlap (different objects of the SAME kind), and 112 different-label overlapping final
pairs (lamp on a table).

| Form | positives ≥ 0.85 | positives ≥ 0.925 | same-kind negatives ≥ 0.85 | cross-kind negatives ≥ 0.85 | best accuracy, any threshold |
|---|---:|---:|---:|---:|---:|
| F0 current .05/.30/.15/.50 | 0.51 | 0.27 | 0.32 | 0.00 | 0.682 |
| F1 soft D, same weights | 0.93 | 0.75 | 0.75 | 0.17 | 0.684 |
| **F2 raw D, .05/.45/.30/.20** | 0.75 | 0.59 | 0.75 | 0.15 | 0.656 |
| F3 soft D, F2 weights | 0.90 | 0.73 | 0.78 | 0.20 | 0.664 |

Raw description cosine: positives median 0.74 (p10 0.49), negatives median 0.57 (p90 0.84).

**Attributes cannot tell two objects of the same kind apart** — every form passes ≥ 32 % of the
same-kind negatives at the association gate, and the best achievable accuracy of the score alone
is 0.66–0.68 for all four forms. They do separate a different kind at the same spot, and the soft
curves lose part of that (17–20 % of lamp-on-table pairs pass). F2 was chosen: it lifts true
re-observations most (medians 0.89–0.99 by group) while keeping cross-kind pairs at a median 0.44.
Consequence for the design: identity must come from geometry; the attribute score is a bounded
channel, not a gate.

### Label similarity is not a kind test (`tools/label_floor.py`, real MiniLM, the run's 40 labels)

pillow/cushion 0.61 and painting/picture 0.41 (one kind) against pillow/bed 0.54, cushion/sofa
0.63, chair/sofa 0.61 (different kinds). No floor separates them, so no label-similarity term was
added; "same kind" in the code is exact base-label string equality, or unknown.

### The GA-328 trap the ruling had to absorb

The evidence engine HOLDS any merge that the overlap channel carries (≥ 50 % of the total) when
co-visibility was never collected for the pair (`PairScore.containment_unchecked`). Two views of
one object are never in one frame together, so this held every genuine re-observation. The guard
exists for cross-kind containment merges (pillow in bed, vanity + bath mat). Under the ruling the
attribute channel is the second witness: the guard is lifted only when that channel is positive
AND the base labels are the same string. Synonyms (painting/picture) and cross-kind pairs stay
held — the reversible failure (rule 11). Self-check: `association.py` section 8.

## Implemented (branch `worktree-found-merge-algorithm`, commits `07cee15`, `b46abb0`; diff in `CHANGES_vs_baseline.diff`)

| Where | What |
|---|---|
| `association.py` | `box_gap`, `geometry_compatible` (intersect, or largest per-axis gap ≤ gap_m), `locality_bounds` (fused box first); `channel_attributes` (bounded: +2.0 cap below the 3.0 commit threshold, −8 floor; abstains without optional evidence; records `same_kind`); `AssocContext.use_ontology / attribute_score_fn / attribute_reference / attribute_max_log_odds / locality_gap_m`; `AssocObject.source`; `score_pair` skips ontology when off, runs attributes; `generate_candidates` also offers gap-compatible pairs (`offered_by: gap`); `containment_unchecked` lifted by the second witness. |
| `object_services.py` | `pair_attribute_score`, `_attribute_channel_input`; constants from config; evidence arm only: room gate refuses only non-overlapping boxes, then `geometry` refusal, then the decision; legacy arm untouched as the ablation baseline. |
| `object_manager_6.py` | `locality_ok(bbox, obj, gap_m)` is the shared test on the fused box; both loops use `association_margin_m` (0.3) and skip a candidate already absorbed in this cycle (same-cycle veto). `TRACKING_IOU_THRESHOLD` is no longer read. |
| `config.yaml`, `config.py` | weights .05/.45/.30/.20; `merge_engine: evidence`; `merge_ontology_channel: false`; `merge_attribute_max_log_odds: 2.0`. |
| tests | `association.py` self-check section 8 (geometry, attributes, second witness); smoke `merge_path_evidence` (hold then merge on the second sweep, lamp-on-table never, room gate on non-overlapping only, geometry refusal at gap 0.5); legacy checks pinned to the legacy arm; smoke 36/37 (GA-128 pre-existing). |

Written and unit-tested; NOT yet run in the simulator. A bundle-replay measurement of both stages
under the new rules is in `verify/` when the review workflow completes.

## Tools

The analysis scripts are in `.handoff/lanes/merge-algorithm/tools/` beside this file:
`assoc_replay.py` (the association counterfactual above), `sweep_c.py` (the C sweep),
`label_floor.py` (label-similarity measurement),
`label_refusals.py` (pair geometry and GT labelling), `dump_pair.py` (raw record check),
`repro_similarity.py` (exact D derivation and repetition count), `room_refusals.py`,
`check_examined_count.py` (the examined-count fix: 16 on the fixed code, 64 on the pre-fix bytes
for 4 identical boxes; both measured).
GT is read only by `label_refusals.py`, after the fact, from
`/DATA/GRAPH-API/.handoff/lanes/perception/ga493_replay_20260914/evaluator/hm3d_00824_run_gt.json`
(SHA-256 `81d12517…`, the file `EVALUATION.json` names).
